doctype html
html(lang="en")
	include ../partials/head

	block content_scripts

	body
		.top-div
			nav.navbar.navbar-inverse
				.container-fluid
					.navbar-header
						a.navbar-brand(href='/')= title.split(' | ')[1]
					ul.nav.navbar-nav.navbar-right
						li
							a(href='/signup')
								span.glyphicon.glyphicon-user
								| Sign Up
						li
							a(href='/')
								span.glyphicon.glyphicon-log-in
								| Login
			p
				i= title.split(' | ')[1]
		.inner-center-div
			p
				i= message

			block content_main
		
		include ../partials/scripts

